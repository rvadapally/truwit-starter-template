---
import BaseHead from '../components/BaseHead.astro';
---

<html lang="en">
  <head>
    <BaseHead title="Truwit — Where Provenance Meets Proof" description="Redefining trust in the age of AI. Truwit attaches cryptographic provenance, consent, and authenticity to digital media." />
    <meta name="description" content="Redefining trust in the age of AI. Truwit attaches cryptographic provenance, consent, and authenticity to digital media.">
    <meta property="og:title" content="Truwit — Where Provenance Meets Proof" />
    <meta property="og:description" content="Redefining trust in the age of AI. Truwit attaches cryptographic provenance, consent, and authenticity to digital media." />
    <meta property="og:image" content="/images/truwit-banner.png" />
    <meta name="twitter:site" content="@TruwitAi" />
  </head>
  <body>
    <main class="main-container">
      <!-- Banner Section -->
      <section class="banner-section">
        <img src="/images/truwit-banner.png" alt="Truwit Banner" class="banner-image" />
      </section>

      <!-- Content Section -->
      <section class="content-section">
        <div class="content-container">
         
          <h1 class="main-headline">
             <span class="gradient-text-primary">Where Provenance Meets Proof</span>
          </h1>
          <p class="tagline">Redefining trust in the age of AI.</p>

          <p class="description">
            Truwit is the lightweight verification layer for digital media—attaching cryptographic provenance,
            consent, and authenticity to AI-generated or user-created content.
          </p>

          <div class="cta-section">
            <a href="/app/" target="_blank" class="cta-button cta-button-primary">
              <svg width="20" height="20" viewBox="0 0 24 24" fill="currentColor" class="cta-icon">
                <path d="M9 12l2 2 4-4m6 2a9 9 0 11-18 0 9 9 0 0118 0z"/>
              </svg>
              Try Verification App
            </a>
            
            <a href="https://x.com/TruwitAi" target="_blank" rel="noopener" class="cta-button cta-button-secondary">
              <svg width="20" height="20" viewBox="0 0 24 24" fill="currentColor" class="cta-icon">
                <path d="M18.244 2.25h3.308l-7.227 8.26 8.502 11.24H16.17l-5.214-6.817L4.99 21.75H1.68l7.73-8.835L1.254 2.25H8.08l4.713 6.231zm-1.161 17.52h1.833L7.084 4.126H5.117z"/>
              </svg>
              Follow updates on X
            </a>
            
            <div class="verified-badge">
              <img src="/images/verified-by-truwit.png" alt="Verified by Truwit" class="verified-icon" />
              <span>Trusted by creators worldwide</span>
            </div>
          </div>

          <footer class="footer">
            <p>© {new Date().getFullYear()} Truwit. All rights reserved.</p>
          </footer>
        </div>
      </section>
    </main>

    <style>
      * {
        margin: 0;
        padding: 0;
        box-sizing: border-box;
      }

      body {
        font-family: ui-sans-serif, system-ui, -apple-system, 'Segoe UI', Roboto, Arial, sans-serif;
        background: radial-gradient(1200px 600px at 70% -10%, #0e1b35 0%, #0b1220 60%);
        color: #e6eefc;
        min-height: 100vh;
        overflow-x: hidden;
      }

      .main-container {
        min-height: 100vh;
        display: flex;
        flex-direction: column;
      }

      .banner-section {
        width: 100%;
        height: 30vh;
        min-height: 200px;
        max-height: 300px;
        overflow: hidden;
        display: flex;
        align-items: center;
        justify-content: center;
      }

      .banner-image {
        width: 100%;
        height: 100%;
        object-fit: cover;
        object-position: center;
      }

      .logo-section {
        display: flex;
        align-items: center;
        justify-content: center;
        gap: 1rem;
        margin-bottom: 1.5rem;
      }

      .logo {
        width: 60px;
        height: 60px;
        filter: drop-shadow(0 0 20px rgba(14, 165, 233, 0.3));
      }

      .brand-name {
        font-weight: 800;
        font-size: 2.5rem;
        letter-spacing: 0.1em;
        text-shadow: 0 0 30px rgba(14, 165, 233, 0.5);
      }

      .main-headline {
        font-size: clamp(1.8rem, 4vw, 3rem);
        line-height: 1.2;
        margin-bottom: 0.75rem;
        font-weight: 600;
        text-shadow: 0 2px 15px rgba(0, 0, 0, 0.3);
      }

      .gradient-text-primary {
        background: linear-gradient(135deg, #0ea5e9, #22c55e);
        -webkit-background-clip: text;
        background-clip: text;
        color: transparent;
        text-shadow: none;
      }

      .gradient-text-secondary {
        background: linear-gradient(135deg, #22c55e, #0ea5e9);
        -webkit-background-clip: text;
        background-clip: text;
        color: transparent;
        text-shadow: none;
      }

      .tagline {
        color: #9fb3d9;
        font-size: clamp(1.2rem, 2.5vw, 1.8rem);
        margin-bottom: 1.5rem;
        font-weight: 300;
        text-shadow: 0 2px 10px rgba(0, 0, 0, 0.3);
      }

      .content-section {
        flex: 1;
        display: flex;
        align-items: center;
        justify-content: center;
        padding: 2rem 2rem;
        background: radial-gradient(1200px 600px at 70% -10%, #0e1b35 0%, #0b1220 60%);
      }

      .content-container {
        max-width: 1000px;
        width: 100%;
        text-align: center;
      }

      .description {
        color: #cfe0ff;
        font-size: clamp(1.1rem, 2vw, 1.3rem);
        line-height: 1.6;
        margin-bottom: 2rem;
        font-weight: 400;
      }

      .cta-section {
        margin-bottom: 1.5rem;
        display: flex;
        flex-direction: column;
        align-items: center;
        gap: 1rem;
      }

      .verified-badge {
        display: flex;
        align-items: center;
        gap: 0.5rem;
        padding: 0.5rem 1rem;
        background: rgba(14, 165, 233, 0.1);
        border: 1px solid rgba(14, 165, 233, 0.2);
        border-radius: 10px;
        color: #0ea5e9;
        font-size: 0.85rem;
        font-weight: 500;
      }

      .verified-icon {
        width: 24px;
        height: 24px;
        filter: brightness(0) saturate(100%) invert(60%) sepia(100%) saturate(2000%) hue-rotate(190deg) brightness(1.2);
      }

      .cta-button {
        display: inline-flex;
        align-items: center;
        gap: 0.75rem;
        padding: 1rem 2rem;
        border-radius: 16px;
        text-decoration: none;
        font-weight: 700;
        font-size: 1.1rem;
        transition: all 0.3s ease;
        border: none;
        cursor: pointer;
      }

      .cta-button-primary {
        color: #08111f;
        background: linear-gradient(135deg, #0ea5e9, #22c55e);
        box-shadow: 0 10px 30px rgba(34, 197, 94, 0.3);
      }

      .cta-button-primary:hover {
        transform: translateY(-2px);
        box-shadow: 0 15px 40px rgba(34, 197, 94, 0.4);
        background: linear-gradient(135deg, #0284c7, #16a34a);
      }

      .cta-button-secondary {
        color: #e6eefc;
        background: rgba(14, 165, 233, 0.15);
        border: 2px solid rgba(14, 165, 233, 0.3);
        box-shadow: 0 10px 30px rgba(14, 165, 233, 0.2);
      }

      .cta-button-secondary:hover {
        transform: translateY(-2px);
        background: rgba(14, 165, 233, 0.25);
        border-color: rgba(14, 165, 233, 0.5);
        box-shadow: 0 15px 40px rgba(14, 165, 233, 0.3);
      }

      .cta-icon {
        width: 20px;
        height: 20px;
      }

      .footer {
        border-top: 1px solid rgba(159, 179, 217, 0.2);
        padding-top: 1.5rem;
      }

      .footer p {
        color: #9fb3d9;
        font-size: 0.9rem;
        font-weight: 300;
      }

      /* Responsive Design */
      @media (max-width: 768px) {
        .banner-section {
          height: 20vh;
          min-height: 150px;
        }

        .content-section {
          padding: 1.5rem 1rem;
        }

        .logo-section {
          margin-bottom: 1rem;
        }

        .logo {
          width: 50px;
          height: 50px;
        }

        .brand-name {
          font-size: 2rem;
        }

        .cta-button {
          padding: 0.875rem 1.5rem;
          font-size: 1rem;
        }

        .cta-section {
          margin-bottom: 1rem;
          gap: 0.75rem;
        }

        .verified-badge {
          padding: 0.4rem 0.8rem;
          font-size: 0.8rem;
        }
      }

      @media (max-width: 480px) {
        .banner-section {
          height: 18vh;
          min-height: 120px;
        }

        .logo-section {
          flex-direction: column;
          gap: 0.5rem;
        }

        .brand-name {
          font-size: 1.75rem;
        }

        .content-section {
          padding: 1rem 1rem;
        }

        .description {
          margin-bottom: 1.5rem;
        }

        .cta-section {
          margin-bottom: 1rem;
          gap: 0.5rem;
        }

        .verified-badge {
          padding: 0.3rem 0.6rem;
          font-size: 0.75rem;
        }
      }
    </style>
  </body>
</html>
